#!/usr/bin/env sh
. ./bin/bootstrap

cd $deDir
git pull

get_current_version
currentVersion=$?

# continue until $n equals 5
while [ $n -le currentVersion ]
do
	echo "Welcome $n times."
	n=$(( n+1 ))	 # increments $n
done
